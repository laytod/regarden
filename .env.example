# Google Calendar (build-time fetch). See README for details.
# GOOGLE_CALENDAR_ICAL_URL=https://calendar.google.com/calendar/ical/.../public/basic.ics

# Deploy to cPanel (npm run deploy). Use cPanel SSH user (FTP username).
# DEPLOY_HOST=your_cpanel_user@regardenus.org
# DEPLOY_PATH=~/public_html
# DEPLOY_SSH_KEY=~/.ssh/regarden
# DEPLOY_SSH_PORT=22
